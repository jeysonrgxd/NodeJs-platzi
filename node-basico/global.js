// no es recomendado usar si podemos evitarlo hay que hacer y trabajar con modulos con las variables globales que tiene node. pero si no crearemos nuestras propias variables globales

global.miVariable = 'Esta es mi variable'
console.log(global);
console.log("-------------------------------")
console.log(miVariable);